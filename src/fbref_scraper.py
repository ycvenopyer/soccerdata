import soccerdata as sd

# create scraper class instance
fbref = sd.FBref()

# create dataframes
# season_stats = fbref.read_team_season_stats(stat_type="shooting")
# print(season_stats.head())

# fbref = sd.FBref(leagues=['INT-European Championship'], seasons=['1900', '2024'])
# Retrieve data for the specified leagues and seasons
# season_stats = fbref.read_team_season_stats(stat_type='shooting')

# print(sd.FBref.available_leagues())
